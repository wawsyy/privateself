# fhevm
fhevmTemp

# directories
.coverage_artifacts
.coverage_cache
.coverage_contracts
build
cache
coverage
dist
node_modules
tmp

# Keep these for collaboration (ABI, types, deployments)
# artifacts/ - tracked for ABI and contract interfaces
# types/ - tracked for TypeScript definitions
# deployments/ - tracked for deployment information

# Next.js build artifacts
.next/

# files
*.env
*.log
.DS_Store
.pnp.*
coverage.json
pnpm-lock.yaml
yarn.lock

